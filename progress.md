Original prompt: Я хочу сделать игру морской бой, на пк, изучи правила этой игры и напиши её на react, с многопользовательским режимом, чтобы люди на разных пк могли играть вместе, сделай её красивой, цветной, анимированной. Напиши план для создания такой игры а так же agents.md с условиями и информацией для ИИ. ИСпользуй только последние версии библиотек

- Подтверждены правила классического морского боя: поле 10x10, флот 5/4/3/3/2, один выстрел за ход, без повторных выстрелов по клетке.
- Проверены актуальные версии основных библиотек через npm registry.
- Репозиторий пустой, проект разворачивается с нуля.
- Созданы `PLAN.md` и `AGENTS.md`.
- Вынесена общая игровая логика в `shared/game.ts`: флот, валидация расстановки, серверное разрешение выстрелов, победа.
- Сделаны экран лобби, создание комнаты, вход по коду, экран расстановки, боевой экран и реванш.
- Проверено: `npm install`, `npm run build`, загрузка UI в браузере, создание комнаты и переход в фазу расстановки.
- TODO: добавить полноценное переподключение игрока, синхронизацию локального draft после реванша, предпросмотр постановки корабля на hover, звуки и e2e сценарий на двух клиентах.
- Архитектура переведена на `PHP 8.2 + MySQL 8.0` без Node-сервера.
- Добавлены `api/index.php`, `api/game.php`, `api/config.php` и `database/schema.sql`.
- Клиент переведён с `Socket.IO` на `fetch` + polling и хранит токен игрока в `localStorage`.
- Проверено: `php -l` для backend-файлов, `npm run build`, успешное PDO-подключение к БД `92.53.99.209:3306`, создание таблиц `rooms` и `room_players`.
- Исправлена утечка координат флота соперника в sanitized room state: массив `ships` теперь не отправляется противнику.
- Временная тестовая комната после проверки API удалена из удалённой БД.
- Добавлена авторасстановка флота через общий генератор в `shared/game/autoPlacement.ts` без автоподтверждения: игрок всё ещё видит preview и вручную подтверждает схему.
- Усилен UX попаданий и потоплений: клетки на своём поле и на радарах теперь кратко вспыхивают по типу события, а в заголовке боя показывается короткий боевой статус.
- Добавлены безбиблиотечные проверки: `npm run test:shared`, `npm run test:contract`, `npm run test:smoke`.
- `test:shared` покрывает `validatePlacements`, `generateRandomPlacements`, `applyShot`, `allowedTargetPlayerId`, `fleetDestroyed`.
- `test:contract` сравнивает одинаковые сценарии TypeScript- и PHP-реализаций правил боя и расстановки.
- `test:smoke` поднимает локальный PHP-сервер и прогоняет жизненный цикл комнаты: create -> join -> submit-setup -> fire -> surrender -> rematch -> cleanup.
- Добавлен браузерный UI-regression слой `npm run test:ui` на `playwright`: проверяет переходы `lobby -> settings -> lobby`, `lobby -> setup -> battle`, действия `auto-place`, `confirm setup`, `surrender`, `finished -> lobby`, а также валится на `pageerror` и неожиданных `console error`.
- В UI добавлены `data-testid` для стабильной автоматизации без хрупкой привязки к текстам и визуальной верстке.
- Проверено: `npm run test:shared`, `npm run test:contract`, `npm run test:smoke`, `npm run build`, `npm run release`.
- Повторно проверено: `npm test` проходит целиком, включая UI-сценарий на собранном релизе.
- Таймер хода на сервере теперь перезапускается не только после промаха и смены игрока, но и после попадания/потопления, когда ход остаётся у того же игрока.
- Во время боя sidebar упрощён: убраны блоки таймера, room-info и статусов игроков; основной блок флота в бою теперь показывает кнопку сдачи и остатки флота противника по типам кораблей.
- Повторно проверено после этих изменений: `npm test`, `npm run release`.
- Добавлена автоматическая очистка старых данных в БД: устаревшие комнаты удаляются по TTL каскадно вместе с `room_players`, а старые `site_presence.player_token` очищаются отдельно.
- Очистка запускается автоматически из PHP API по редкому trigger и может быть принудительно включена через `APP_FORCE_STORAGE_CLEANUP=1`.
- Добавлен `npm run test:cleanup`, который создаёт устаревшие записи, запускает cleanup и проверяет удаление старой комнаты, игроков и presence-токена без затрагивания свежих данных.
- Добавлен отдельный режим `игра с ботом`: в лобби появилась кнопка создания bot-room, сервер создаёт специальную комнату `человек + бот`, а клиент получает обычный sanitized room state без раскрытия лишних данных.
- Бот полностью серверный: сам расставляет флот, автоматически стартует бой после подтверждения расстановки человеком, участвует в реванше и не ломает обычные комнаты с реальными игроками.
- Логика выстрелов бота сделана в два режима одновременно: поиск по шахматному паттерну и добивание после попадания, когда бот последовательно продолжает уже найденный корабль до потопления.
- Расширены проверки: `test:smoke` теперь покрывает жизненный цикл bot-room, а `test:ui` проверяет React-переход `lobby -> setup -> battle -> finished -> lobby` и для игры с ботом.
- TODO: при следующем цикле стоит вынести battle-feedback в более формальный event-stream, чтобы не вычислять анимации по diff комнаты на клиенте.
